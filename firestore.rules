rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Allow read/write to all authenticated or open (adjust as needed)
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
